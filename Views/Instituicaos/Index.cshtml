@model IEnumerable<Academico_Dependencia.Models.Instituicao>

@{
    ViewData["Title"] = "Index";
}

<h1>Cadastro</h1>

<p>
    <a asp-action="Create">Novo Cadastro</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Endereco)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Endereco)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.InstituicaoID">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.InstituicaoID">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.InstituicaoID">Excluir</a>
            </td>
        </tr>
}
    </tbody>
</table>
